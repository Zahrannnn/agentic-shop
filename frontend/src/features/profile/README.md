# Profile Feature

Starter settings/profile feature demonstrating TanStack Form, Zod validation, mutation hooks, and local adapter boundaries.

- Keep schemas in `validations/`.
- Keep fake or real persistence behind `api/`.
- Components receive data and callbacks from hooks.
