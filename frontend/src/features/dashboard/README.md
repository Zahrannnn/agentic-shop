# Dashboard Feature

Starter product dashboard showing metrics, query hooks, table composition, and client preference state.

- API-like work belongs in `api/`, even when the adapter is fake.
- Components stay presentational.
- Hooks own data fetching and state coordination.
